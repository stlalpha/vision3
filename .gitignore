# If you prefer the allow list template instead of the deny list, see community template:
# https://github.com/github/gitignore/blob/main/community/Golang/Go.AllowList.gitignore
#
# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib
/log
/strings
/ue
/config

bin/
DOS/
/doors/

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# Dependency directories (remove the comment below to include it)
# vendor/

# Go workspace file
go.work
go.work.sum

# env file
.env

# Runtime data directory (ignore contents, keep .gitkeep)
/data/*
!/data/.gitkeep

# Compiled executable
/vision3
/cmd/vision3/vision3
/cmd/ansitest/ansitest
/v3mail
/helper
tmp/
.gocache/

# Config files (contain site-specific settings, FTN addresses, passwords)
/configs/*.json
/configs/*.txt

# FTN NA (nodelist area) files — site-specific, not tracked in git
*.na

# SSH host keys (generated during setup, not tracked in git)
/configs/ssh_host_*

# Logs and debug files
*.log
log/
logs/

# Backup files
*.bak
*.backup
*.old
*.new
*.orig
*.tmp

# IDE and editor files
.idea/
.vscode/
*.swp
*.swo
*~

# OS files
.DS_Store
Thumbs.db

# Cursor rules (local development)
.cursorrules

# Claude Code worktrees
.claude/worktrees/
