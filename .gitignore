# If you prefer the allow list template instead of the deny list, see community template:
# https://github.com/github/gitignore/blob/main/community/Golang/Go.AllowList.gitignore
#
# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib
log

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# Dependency directories (remove the comment below to include it)
# vendor/

# Go workspace file
go.work
go.work.sum

# env file
.env

# Runtime data directory
/data/
/data/*
!.gitkeep

# Distribution directory - exclude all build artifacts
/dist/
/dist/*
!.gitkeep

# Compiled executables and build artifacts
/vision3
/vision3-config
/cmd/vision3/vision3
/cmd/ansitest/ansitest
/cmd/strings-decoder/strings-decoder
/cmd/vision3-config/vision3-config

# Build directories
/dist-release/
/dist-release/*

# SSH host keys (all of them, including public keys)
configs/ssh_host_*
!configs/ssh_host_keys.example

# GPG keys and signatures (never commit private keys)
*.asc
*.sig
*.gpg
!vision3-signing-key.asc
SIGNATURES.txt

# Logs and debug files
*.log
log/
logs/
tea_debug.log
config-debug.log
debug.log
full-debug.log
urmom.log
wut.log

# Backup files
*.bak
*.backup
*.old
*.new
*.orig
*.tmp

# IDE and editor files
.idea/
.vscode/
*.swp
*.swo
*~

# OS files
.DS_Store
Thumbs.db

# Cursor rules (local development)
.cursorrules
